[build]
builder = "NIXPACKS"

[deploy]
startCommand = "playwright install chromium && playwright install-deps chromium && python wisdom_sync.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
